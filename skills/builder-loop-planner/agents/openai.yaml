interface:
  display_name: "Builder Loop Planner"
  short_description: "生成并验证 Full Driver v4 实验交付方案"
  default_prompt: "Use $builder-loop-planner to prepare a validated Full Driver v4 experiment plan."

policy:
  allow_implicit_invocation: true
