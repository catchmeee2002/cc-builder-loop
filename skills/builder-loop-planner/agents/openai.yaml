interface:
  display_name: "Builder Loop Planner"
  short_description: "维护中：业务规划统一使用 Codex 原生 Plan"
  default_prompt: "Use native Codex Plan because Builder Loop planning is disabled during the v4 rebuild."

policy:
  allow_implicit_invocation: false
