interface:
  display_name: "Builder Loop"
  short_description: "维护中：保留旧交付诊断，暂停创建业务 run"
  default_prompt: "Report that Builder Loop is in maintenance mode and do not start a new run."

policy:
  allow_implicit_invocation: false
