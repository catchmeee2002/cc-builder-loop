interface:
  display_name: "Builder Loop"
  short_description: "执行已验证的 Full Driver v4 实验方案"
  default_prompt: "Use $builder to execute the accepted Full Driver v4 experiment plan."

policy:
  allow_implicit_invocation: true
