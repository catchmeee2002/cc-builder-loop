interface:
  display_name: "Builder Loop"
  short_description: "按独立测试、机器验证、黑盒复验与审查门禁交付代码变更"
  default_prompt: "Use $builder to deliver the accepted plan through independent tests, verification, and review."

policy:
  allow_implicit_invocation: false
