interface:
  display_name: "Record GitHub Issue"
  short_description: "记录 GitHub Issue 的事故快照与最终结案证据"
  default_prompt: "Use $file-github-issue to create, update, or close this GitHub Issue with auditable evidence."

policy:
  allow_implicit_invocation: true
