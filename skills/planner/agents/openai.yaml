interface:
  display_name: "Planner"
  short_description: "独立规划，可交给 Codex 原生或 Builder adapter"
  default_prompt: "Use $planner to prepare an implementation plan for native Codex execution or an explicit Builder-loop handoff."

policy:
  allow_implicit_invocation: true
