<!-- BEGIN cc-builder-loop-codex -->
## Codex Builder Loop Maintenance

- Builder-loop 正在重建 v4 交付核心。维护期内所有代码和文档规划都使用 Codex 原生 Plan，不显示
  Builder-loop 路线选择，不加载 `$builder-loop-planner`，也不输出任何 Builder handoff marker。
- `$builder`、原生“实施计划”动作和历史 marker 都不得创建新的 Builder-loop run；若 Skill 被显式或
  隐式加载，必须在 runtime、计划物化和仓库写入前返回维护状态并停止。
- legacy v2/v3 CLI 仅保留既有 ledger 的 `status`、`doctor`、`recover`、`finalize` 与安全 cleanup。
  新 run 只允许仓库内部确定性测试通过显式环境开关创建，不能用于业务交付。
- 普通任务继续使用 Codex 原生执行能力。Full Driver v4 通过历史回放和跨项目真实验收后，才恢复
  Plan 环节的一次性高保证交付选项；用户不需要学习 v4、Core 或新的业务命令。
<!-- END cc-builder-loop-codex -->
