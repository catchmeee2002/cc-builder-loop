<!-- BEGIN cc-builder-loop-codex -->
## Codex Builder Loop Experiment

- Plan mode 入口规则：涉及代码或文档变更且路线尚未确定时，第一条动作必须用 `request_user_input`
  只询问「Planner + Codex 原生执行」或「Builder-loop 实验」；回答前不得读取、分析、输出结论或提出其他问题。
- 选择「Planner + Codex 原生执行」时加载 `$planner`，只产出普通 Codex 实施计划；不得加载
  `$builder-loop-planner`，不得生成 Assurance v4 contract、`BUILDER_HANDOFF_READY` 或启动 run。
- 选择「Builder-loop 实验」时调用 `$builder-loop-planner` adapter；它消费通用 Planner 的语义计划，
  只产出并验证 Assurance v4 contract，`READY` 后在方案外输出独立行 `BUILDER_HANDOFF_READY`，
  不得实现或启动 run。
- 若同一 session 的紧邻下一轮已处于 Default mode，且用户通过 Codex 原生“实施计划”动作提交
  `Implement the plan.`，仅当上一轮存在 `BUILDER_HANDOFF_READY` 时，该动作才等价于对上述实验方案
  显式调用 `$builder`，直接加载 Builder Skill；普通 `$planner` 计划继续由 Codex 原生能力执行，
  不重复授权。
- 显式 `$builder` 继续作为手工入口。缺少就绪标记、不是紧邻下一轮、仍在 Plan mode、contract 未验证
  或来自 Codex 原生 Plan 时，不得把普通实施请求隐式转成 Builder-loop。
- `$builder` 只桥接 Full Driver v4 实验；legacy v2/v3 新 run 和未带 `--experimental-v4` 的 Assurance
  入口继续关闭。Full Driver 内部 Skill 不作为用户额外学习入口，也不得被普通请求隐式调用。
<!-- END cc-builder-loop-codex -->
