<!-- BEGIN cc-builder-loop-codex -->
## Codex Builder Loop

- 使用 `/plan` 为后续交付制定方案时，必须调用 `$builder-loop-planner`；只产出并验证计划，随后停止并提示用户显式调用 `$builder`，不得实现或启动 run。
- 只有用户显式输入 `$builder` 时才启动 builder-loop；普通改码请求不要隐式触发。
<!-- END cc-builder-loop-codex -->
