# 审查报告
**时间**：2026-04-26 16:22  **范围**：run-judge-agent.sh / test-judge-env-file-load.sh / test-judge-model-fallback.sh / judge-env.sh.example / loop.schema.yml / CLAUDE.md / SKILL.md / README.md / docs/judge-agent.md / known-risks.md / .claude/loop.yml / .gitignore  **深度**：深度

| 级别 | 位置 | 问题及建议 |
|------|------|-----------|
| 🟡 | run-judge-agent.sh:573-583 | `run_self_check()` 的 missing credentials HINT 仍是 V1.9 旧文案（仅提 console.anthropic.com 独立 key），未提 V2.1 judge-env.sh 方案；--self-check 是用户第一诊断入口，应与 CLAUDE.md 7.3 / README.md 同步更新 |
| 🟡 | loop.schema.yml:201 | `credentials_file` 的 `default: "~/.claude/skills/builder-loop/judge-env.sh"` 与代码默认空字符串不一致：代码 read_judge_config 缺字段返回空，phase 0 独立 source 全局路径；用户若按 schema 在 loop.yml 显式填写同一路径，反而触发 phase 1 的 `!= JUDGE_ENV_FILE_DEFAULT` 跳过逻辑，行为与期望相反；建议改 default 为空字符串并在 description 说明全局路径由 phase 0 自动处理 |
| 🟡 | run-judge-agent.sh:755,770 | `FALLBACK_TRIGGERED` 变量只赋值（false/true）从不读取，是死代码；无功能影响但混淆阅读，可删除或保留为 telemetry 字段 |
| 🔵 | test-judge-model-fallback.sh:582 | B3 fallback retry 也失败时，仅断言 `downgraded=True`，未断言 `downgrade_reason` 含 `fallback_also_failed`；建议加 `assert_json "B3 downgrade_reason 含 fallback_also_failed" ...` 精确覆盖 fallback_also_failed 路径 |
| 🔵 | merge-worktree-back.sh（未在本 diff 内） | auto-commit 使用 `git add -A` 会把 telemetry `.claude/builder-loop/judge-trace.jsonl` 等运行时文件提交进 worktree branch，本次靠 .gitignore 兜底；根因未改，未来若有新运行时文件未加 .gitignore 会再次复现；建议改为 `git add -- <source_dirs>` 精确 add，排除 .claude/ 目录 |
| 🔵 | test-judge-model-fallback.sh:502 | `make_loop_yml` 第 4 参数 `$4` 命名为 `threshold` 但语义是 `fallback_after_failures`；调用处传的是数字 2，无歧义，但函数签名注释不清晰 |

**结论**：需修改
**必须解决**：
- 🟡 run-judge-agent.sh:573 — --self-check HINT 文案与 V2.1 文档不一致，用户跑 self-check 会拿到过时指引
- 🟡 loop.schema.yml:201 — credentials_file default 值与代码实际默认行为矛盾
- 🟡 run-judge-agent.sh:755 — FALLBACK_TRIGGERED 死变量（低优先级，但应清理）
