# 审查报告
**时间**：2026-05-08 15:33  **范围**：install.sh, diagnose-stop-hook.sh, test-plan-detection.sh, .claude/improvements.md, .claude/plans/20260508-plan-detection.md  **深度**：快速

| 级别 | 位置 | 问题及建议 |
|------|------|-----------|
| 🔴 | diagnose-stop-hook.sh:147-157 | [2/6] 软链 expected 列表未按 PLAN 过滤：tester-write-guard.sh 硬编码在内，max 方案下不存在时 LINKS_VERDICT=fail → VERDICT=2 → fixture Case 2 ec!=2 断言失败。需与 [1/6] 同样按 PLAN 过滤软链列表 |
| 🟡 | install.sh:175 | 完成文案硬编码列出 tester-write-guard.sh，max 方案下不准确，应改为动态或按方案输出 |
| 🟡 | install.sh:91-92 | python3 here-doc 通过 env PLAN 传变量，和 set -euo pipefail 外层 bash 兼容，但 heredoc 默认 fallback 写 `plan = os.environ.get("PLAN", "copilot")` —— max 方案下若 env 传递失败会静默降级为 copilot 行为，风险可接受但 fallback 语义值得注释说明 |
| 🔵 | test-plan-detection.sh:169 | ≤1 容忍设计合理（A 批未合 main 的 uninstall.sh bug），但注释中的"A 批 cherry-pick 后会到 0"是时间依赖性假设，建议在 A 批合 main 时改回严格 0 并删除容忍注释 |
| 🔵 | install.sh:23-24 / diagnose:30-31 | plan_filter 当前只有 "" 和 "copilot"，未来若加 gemini/openrouter 等方案需扩展为集合匹配，当前实现够用但可在注释里预留扩展说明 |
| 🔵 | improvements.md 追加 | 两条 c1+c2 候选条目随 stash apply 带入 commit 不算噪音，内容本身价值高（多 worktree 静默丢失 + WIP 节流），符合改动相关性要求 |

**关于 reviewer 提问**

1. **detect_plan 一致性**：install.sh L21-27 和 diagnose L28-34 完全相同，一致，无问题。
2. **registrations plan_filter 写法**：清晰，元组第 4 字段 + 注释说明 + 主循环 skip 计数，可读性好。
3. **ASCII 输出文案回避**：合理，本期不触发 c1 的中文括号炸点是正确的防御策略；c1 是独立任务，不应为回避而顺手做（会引入 scope 蔓延）。
4. **uninstall ≤1 容忍**：合理但有时间债，参见 🔵 建议。
5. **plan_filter 扩展性**：当前 2 值够用，参见 🔵 建议。
6. **improvements.md / plan 文件随 commit 带入**：不算噪音，计划文件和立项条目本来就是本次改动的配套产物。

**结论**：需修改  
**必须解决**：diagnose-stop-hook.sh [2/6] 软链列表未按 PLAN 过滤（导致 max 方案下 fixture 失败，是 c2 核心目标未完整落地）
