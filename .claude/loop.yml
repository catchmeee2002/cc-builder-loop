pass_cmd:
  - stage: test
    cmd: "python3 -m pytest -p no:html -p no:cacheprovider -q tests"
    timeout: 600
max_iterations: 5
proof_runner:
  framework: pytest
  cmd: "python3 -m pytest -p no:html"
